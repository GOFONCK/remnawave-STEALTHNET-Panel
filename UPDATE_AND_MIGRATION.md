# 📦 Руководство по обновлению и миграции базы данных

Это руководство описывает процесс обновления проекта StealthNET VPN и выполнения миграций базы данных.

## 📋 Содержание

- [Обновление проекта](#обновление-проекта)
- [Миграции базы данных](#миграции-базы-данных)
- [Доступные миграции](#доступные-миграции)
- [Процесс обновления с миграцией](#процесс-обновления-с-миграцией)
- [Резервное копирование](#резервное-копирование)
- [Откат изменений](#откат-изменений)
- [Часто задаваемые вопросы](#часто-задаваемые-вопросы)

---

## 🔄 Обновление проекта

### Обновление через Git

Если проект хранится в Git репозитории:

```bash
# 1. Создайте резервную копию базы данных
cp instance/stealthnet.db instance/stealthnet.db.backup_$(date +%Y%m%d_%H%M%S)

# 2. Остановите Flask приложение
sudo systemctl stop flask_api  # или ваш способ остановки

# 3. Обновите код
git pull origin main  # или master, или ваша ветка

# 4. Обновите зависимости (если изменились requirements.txt)
source venv/bin/activate  # активируйте виртуальное окружение
pip install -r requirements.txt --upgrade

# 5. Выполните миграции (см. раздел ниже)
python3 migrate_payment_systems.py  # если нужно

# 6. Пересоберите фронтенд (если изменился)
cd admin-panel
npm install
npm run build
cd ..

# 7. Перезапустите Flask приложение
sudo systemctl start flask_api
```

### Обновление вручную

Если проект обновляется вручную:

```bash
# 1. Создайте резервную копию
cp -r /path/to/project /path/to/project.backup_$(date +%Y%m%d_%H%M%S)
cp instance/stealthnet.db instance/stealthnet.db.backup_$(date +%Y%m%d_%H%M%S)

# 2. Остановите приложение
sudo systemctl stop flask_api

# 3. Замените файлы проекта новыми версиями

# 4. Выполните миграции
python3 migrate_payment_systems.py

# 5. Перезапустите приложение
sudo systemctl start flask_api
```

---

## 🗄️ Миграции базы данных

### Что такое миграция?

Миграция — это процесс изменения структуры базы данных (добавление новых колонок, таблиц) без потери существующих данных.

### Когда нужна миграция?

Миграция необходима, когда:
- Добавлены новые платежные системы (Platega, Mulenpay, UrlPay)
- Добавлены новые поля в существующие таблицы
- Изменена структура таблиц

### Автоматическая инициализация

При первом запуске `app.py` автоматически создается база данных со всеми необходимыми таблицами и колонками. Миграции нужны только для **существующих** баз данных.

---

## 📝 Доступные миграции

### 1. Миграция платежных систем (`migrate_payment_systems.py`)

**Назначение:** Добавляет колонки для новых платежных систем (Platega, Mulenpay, UrlPay) в таблицу `payment_setting`.

**Добавляемые колонки:**
- `platega_api_key` (TEXT)
- `platega_merchant_id` (TEXT)
- `mulenpay_api_key` (TEXT)
- `mulenpay_secret_key` (TEXT)
- `mulenpay_shop_id` (TEXT)
- `urlpay_api_key` (TEXT)
- `urlpay_secret_key` (TEXT)
- `urlpay_shop_id` (TEXT)

**Запуск:**
```bash
python3 migrate_payment_systems.py
```

**Что делает скрипт:**
1. Находит базу данных (`instance/stealthnet.db` или `stealthnet.db`)
2. Проверяет существующие колонки в таблице `payment_setting`
3. Добавляет недостающие колонки
4. Создает резервную копию базы данных
5. Выводит отчет о выполненных изменениях

**Пример вывода:**
```
📦 Найдена база данных: /var/www/stealthnet-api/instance/stealthnet.db
📋 Существующие колонки в payment_setting: id, crystalpay_api_key, ...

➕ Добавляем колонку platega_api_key...
✓ Колонка platega_api_key добавлена
➕ Добавляем колонку platega_merchant_id...
✓ Колонка platega_merchant_id добавлена
...

✅ Миграция успешно завершена!
📝 Резервная копия сохранена: instance/stealthnet.db.backup_1234567890
```

### 2. Миграция YooKassa (`migrations/migrate_add_yookassa_fields.py`)

**Назначение:** Добавляет колонки для платежной системы YooKassa.

**Добавляемые колонки:**
- `yookassa_shop_id` (TEXT)
- `yookassa_secret_key` (TEXT)

**Запуск:**
```bash
python3 migrations/migrate_add_yookassa_fields.py
```

Или через Flask CLI:
```bash
python3 -m flask --app app migrate-yookassa-fields
```

### 3. Другие миграции

В папке `migrations/` находятся дополнительные миграции:
- `migrate_add_heleket.py` — добавление Heleket
- `migrate_add_telegram_fields.py` — добавление полей Telegram
- `migrate_add_telegram_bot_token.py` — добавление токена бота
- `migrate_add_promo_code_id.py` — добавление связи с промокодами
- `migrate_add_badge.py` — добавление поля badge в тарифы

---

## 🔧 Процесс обновления с миграцией

### Шаг 1: Подготовка

```bash
# 1. Создайте резервную копию базы данных
cp instance/stealthnet.db instance/stealthnet.db.backup_$(date +%Y%m%d_%H%M%S)

# 2. Остановите Flask приложение
sudo systemctl stop flask_api
# или
pm2 stop flask_api
# или другой способ остановки
```

### Шаг 2: Обновление кода

Обновите файлы проекта (через Git, FTP, или другим способом).

### Шаг 3: Выполнение миграций

```bash
# Активируйте виртуальное окружение (если используется)
source venv/bin/activate

# Выполните миграцию платежных систем
python3 migrate_payment_systems.py

# Если нужно, выполните другие миграции
python3 migrations/migrate_add_yookassa_fields.py
```

### Шаг 4: Проверка

```bash
# Проверьте, что миграция прошла успешно
sqlite3 instance/stealthnet.db "PRAGMA table_info(payment_setting);"
```

Должны быть видны все новые колонки.

### Шаг 5: Перезапуск

```bash
# Перезапустите Flask приложение
sudo systemctl start flask_api

# Проверьте логи
sudo journalctl -u flask_api -f
# или
tail -f /var/log/flask_api.log
```

### Шаг 6: Проверка работы

1. Откройте админ-панель: `https://ваш-домен/admin`
2. Перейдите в раздел "Настройки платежных систем" (`/admin/payments`)
3. Убедитесь, что новые поля отображаются
4. Проверьте, что старые данные сохранены

---

## 💾 Резервное копирование

### Автоматическое резервное копирование

Скрипт `migrate_payment_systems.py` автоматически создает резервную копию перед изменениями:
```
instance/stealthnet.db.backup_1234567890
```

### Ручное резервное копирование

```bash
# Полная резервная копия базы данных
cp instance/stealthnet.db instance/stealthnet.db.backup_$(date +%Y%m%d_%H%M%S)

# Или с указанием конкретной даты
cp instance/stealthnet.db instance/stealthnet.db.backup_20240115_120000
```

### Восстановление из резервной копии

```bash
# Остановите приложение
sudo systemctl stop flask_api

# Восстановите базу данных
cp instance/stealthnet.db.backup_20240115_120000 instance/stealthnet.db

# Перезапустите приложение
sudo systemctl start flask_api
```

---

## ⏪ Откат изменений

### Откат миграции

Если миграция прошла неудачно или нужно откатить изменения:

```bash
# 1. Остановите приложение
sudo systemctl stop flask_api

# 2. Восстановите базу данных из резервной копии
cp instance/stealthnet.db.backup_XXXXX instance/stealthnet.db

# 3. Откатите изменения в коде (если нужно)
git checkout HEAD~1  # если используете Git

# 4. Перезапустите приложение
sudo systemctl start flask_api
```

**Важно:** SQLite не поддерживает удаление колонок напрямую. Если нужно удалить колонку, потребуется:
1. Создать новую таблицу без этой колонки
2. Скопировать данные
3. Удалить старую таблицу
4. Переименовать новую таблицу

---

## ❓ Часто задаваемые вопросы

### Q: Нужно ли выполнять миграцию при первом запуске?

**A:** Нет. При первом запуске `app.py` автоматически создает базу данных со всеми необходимыми таблицами и колонками. Миграции нужны только для существующих баз данных.

### Q: Что делать, если миграция не работает?

**A:** 
1. Проверьте, что база данных существует: `ls -la instance/stealthnet.db`
2. Проверьте права доступа: `chmod 644 instance/stealthnet.db`
3. Убедитесь, что Flask приложение остановлено
4. Проверьте логи ошибок в консоли
5. Восстановите из резервной копии и попробуйте снова

### Q: Можно ли выполнять миграцию на работающем сервере?

**A:** Технически можно, но **не рекомендуется**. Лучше остановить приложение на время миграции, чтобы избежать конфликтов.

### Q: Что делать, если после миграции появились ошибки?

**A:**
1. Проверьте логи приложения: `sudo journalctl -u flask_api -n 100`
2. Убедитесь, что все колонки добавлены: `sqlite3 instance/stealthnet.db "PRAGMA table_info(payment_setting);"`
3. Если ошибки критичны, откатите изменения из резервной копии
4. Проверьте, что код приложения соответствует структуре базы данных

### Q: Как проверить, какие миграции уже выполнены?

**A:**
```bash
# Проверьте структуру таблицы payment_setting
sqlite3 instance/stealthnet.db "PRAGMA table_info(payment_setting);"

# Или через Python
python3 -c "import sqlite3; conn = sqlite3.connect('instance/stealthnet.db'); cursor = conn.cursor(); cursor.execute('PRAGMA table_info(payment_setting)'); print([col[1] for col in cursor.fetchall()])"
```

### Q: Нужно ли выполнять все миграции по порядку?

**A:** Нет. Каждая миграция независима и проверяет наличие колонок перед добавлением. Можно выполнять только нужные миграции.

### Q: Что делать после обновления кода без изменений в БД?

**A:** 
1. Создайте резервную копию (на всякий случай)
2. Обновите код
3. Перезапустите приложение
4. Миграции выполнять не нужно

---

## 📌 Важные замечания

### ⚠️ Перед миграцией

1. **Всегда создавайте резервную копию** базы данных
2. **Останавливайте Flask приложение** на время миграции
3. **Проверяйте версию кода** — убедитесь, что используете актуальную версию
4. **Читайте логи** миграции внимательно

### ✅ После миграции

1. **Проверьте структуру БД** — убедитесь, что все колонки добавлены
2. **Перезапустите приложение** — обязательно перезапустите Flask
3. **Проверьте работу** — откройте админ-панель и убедитесь, что все работает
4. **Сохраните резервную копию** — не удаляйте резервные копии сразу

### 🔒 Безопасность

- Резервные копии базы данных содержат чувствительные данные (зашифрованные ключи API)
- Храните резервные копии в безопасном месте
- Не передавайте резервные копии по незащищенным каналам
- Регулярно удаляйте старые резервные копии (но не все сразу)

---

## 📚 Дополнительные ресурсы

- [README.md](../README.md) — основная документация проекта
- [MIGRATION_INSTRUCTIONS.md](./MIGRATION_INSTRUCTIONS.md) — детальные инструкции по миграции badge
- [migrate_payment_systems.py](../migrate_payment_systems.py) — скрипт миграции платежных систем

---

## 🆘 Поддержка

Если у вас возникли проблемы с обновлением или миграцией:

1. Проверьте логи приложения
2. Проверьте структуру базы данных
3. Восстановите из резервной копии
4. Обратитесь к документации проекта

---

**Последнее обновление:** 2024-01-15  
**Версия:** 1.0

